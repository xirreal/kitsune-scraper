name: kitsune_scraper
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  fluent_ui: ^3.9.1
  window_manager: ^0.1.7
  system_theme: ^1.0.1
  http: ^0.13.4
  path_provider: ^2.0.9
  retry: ^3.1.0
  windows_taskbar: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  
